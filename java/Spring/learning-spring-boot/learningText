dependency injection :-
delegation of creation of object to frame work rather than u manually creating it.

@Service
(creating singleton of the class)


@Inject
(to get the instance of the class)

@RestController

@RequestMapping

@Service
@Component
@Controller
@Repository
find the difference between all of them.

@Autowired
(learn the diff between method/constructor autowire and field autowiring)

